apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "20000"
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - port: 20000
      targetPort: 20000